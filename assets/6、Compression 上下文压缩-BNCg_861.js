import{a as e,c as t,i as n}from"./app-Cqbiks9-.js";import{t as r}from"./plugin-vue_export-helper-BDNMzG2s.js";var i=JSON.parse(`{"path":"/code/AI/AI%20Agent/Claude-code-Harness/6%E3%80%81Compression%20%E4%B8%8A%E4%B8%8B%E6%96%87%E5%8E%8B%E7%BC%A9.html","title":"Compression 上下文压缩","lang":"en-US","frontmatter":{"description":"Compression 上下文压缩 整体交互流程 三层压缩按从轻到重的顺序依次触发：先处理单条大输出，再压缩历史工具结果，最后才动用模型做完整摘要。 此外，agent 自身可以调用 compact 工具手动触发第三层压缩（无需等到上下文超限）。 实现思路 压缩模块位于 src/compression/，核心文件： loop.py 中的 turn() 函...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Compression 上下文压缩\\",\\"image\\":[\\"\\"],\\"dateModified\\":\\"2026-05-05T13:51:59.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"T4mako\\",\\"url\\":\\"https://github.com/T4mako/T4mako.github.io\\"}]}"],["meta",{"property":"og:url","content":"https://t4mako.github.io/code/AI/AI%20Agent/Claude-code-Harness/6%E3%80%81Compression%20%E4%B8%8A%E4%B8%8B%E6%96%87%E5%8E%8B%E7%BC%A9.html"}],["meta",{"property":"og:title","content":"Compression 上下文压缩"}],["meta",{"property":"og:description","content":"Compression 上下文压缩 整体交互流程 三层压缩按从轻到重的顺序依次触发：先处理单条大输出，再压缩历史工具结果，最后才动用模型做完整摘要。 此外，agent 自身可以调用 compact 工具手动触发第三层压缩（无需等到上下文超限）。 实现思路 压缩模块位于 src/compression/，核心文件： loop.py 中的 turn() 函..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"property":"og:updated_time","content":"2026-05-05T13:51:59.000Z"}],["meta",{"property":"article:modified_time","content":"2026-05-05T13:51:59.000Z"}]]},"git":{"createdTime":1777989119000,"updatedTime":1777989119000,"contributors":[{"name":"T4mako","username":"T4mako","email":"1536954632@qq.com","commits":1,"url":"https://github.com/T4mako"}]},"readingTime":{"minutes":4.95,"words":1485},"filePathRelative":"code/AI/AI Agent/Claude-code-Harness/6、Compression 上下文压缩.md","excerpt":"","autoDesc":true}`),a={name:`6、Compression 上下文压缩.md`};function o(r,i,a,o,s,c){return t(),n(`div`,null,[...i[0]||=[e(`<h1 id="compression-上下文压缩" tabindex="-1"><a class="header-anchor" href="#compression-上下文压缩"><span>Compression 上下文压缩</span></a></h1><h2 id="整体交互流程" tabindex="-1"><a class="header-anchor" href="#整体交互流程"><span>整体交互流程</span></a></h2><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-"><span class="line"><span>工具执行完成 → persist_large_output() (第一层：大输出写盘)</span></span>
<span class="line"><span>       ↓</span></span>
<span class="line"><span>进入下一轮 turn → pre_api_compress() (第二层：旧工具结果压缩)</span></span>
<span class="line"><span>       ↓</span></span>
<span class="line"><span>   估算上下文大小 &gt; CONTEXT_LIMIT？</span></span>
<span class="line"><span>       ├─ 否 → 正常调用 API</span></span>
<span class="line"><span>       └─ 是 → compact_history() (第三层：模型摘要压缩)</span></span>
<span class="line"><span>                     ↓</span></span>
<span class="line"><span>                写transcript → 调用模型摘要 → 用单条 user 消息替换全部历史</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>三层压缩按<strong>从轻到重</strong>的顺序依次触发：先处理单条大输出，再压缩历史工具结果，最后才动用模型做完整摘要。</p><p>此外，agent 自身可以调用 <code>compact</code> 工具<strong>手动触发</strong>第三层压缩（无需等到上下文超限）。</p><h2 id="实现思路" tabindex="-1"><a class="header-anchor" href="#实现思路"><span>实现思路</span></a></h2><p>压缩模块位于 <code>src/compression/</code>，核心文件：</p><table><thead><tr><th>文件</th><th>职责</th></tr></thead><tbody><tr><td><code>src/compression/context.py</code></td><td>三层压缩策略的全部实现 + 压缩状态管理</td></tr><tr><td><code>src/utils/compress_prompt.py</code></td><td>摘要压缩的 system prompt</td></tr><tr><td><code>src/tools/registry.py</code></td><td><code>compact</code> 工具元信息 + 占位符 handler</td></tr></tbody></table><p><code>loop.py</code> 中的 <code>turn()</code> 函数是压缩的<strong>触发点</strong>：</p><ol><li><strong>每轮 API 调用前</strong>自动执行 <code>pre_api_compress()</code>（包含第一层和第二层）</li><li><strong>工具执行后</strong>对结果调用 <code>persist_large_output()</code>（第一层）</li><li><strong>工具执行后</strong>如果是 <code>read_file</code>，记录文件路径到 <code>CompactState</code>（压缩后恢复用）</li><li><strong>工具执行后</strong>如果是 <code>compact</code>，执行 <code>compact_history()</code>（第三层手动触发）</li></ol><p>压缩状态通过全局单例 <code>COMPACT_STATE = CompactState()</code> 维护，记录是否已压缩、上次摘要内容、最近读取的文件列表。</p><h2 id="核心问题" tabindex="-1"><a class="header-anchor" href="#核心问题"><span>核心问题</span></a></h2><h3 id="q1-上下文压缩有哪些方式" tabindex="-1"><a class="header-anchor" href="#q1-上下文压缩有哪些方式"><span>Q1: 上下文压缩有哪些方式？</span></a></h3><p>三种方式，对应三个层次，<strong>成本递增，压缩力度递增</strong>：</p><table><thead><tr><th>层级</th><th>函数</th><th>方式</th><th>成本</th><th>压缩力度</th></tr></thead><tbody><tr><td>第一层</td><td><code>persist_large_output()</code></td><td>大输出写盘，上下文只保留预览</td><td>零模型调用</td><td>单条输出从 30k+ 缩到 ~2k</td></tr><tr><td>第二层</td><td><code>micro_compact()</code></td><td>旧工具结果替换为一行占位符</td><td>零模型调用</td><td>每条旧结果缩为一行</td></tr><tr><td>第三层</td><td><code>compact_history()</code></td><td>调用模型生成结构化摘要，替换整个对话历史</td><td>一次 API 调用</td><td>数百条消息缩为一条</td></tr></tbody></table><p><strong>第一层：持久化大输出 (<code>persist_large_output</code>)</strong></p><p>当单条工具输出超过 <code>PERSIST_THRESHOLD</code> (30,000 字符) 时，将完整内容写入磁盘文件 (<code>{tool_use_id}.txt</code>)，在上下文中只保留 <code>&lt;persisted-output&gt;</code> 标签包裹的 2,000 字符预览 + 文件路径。小输出原样返回，不做处理。</p><p>关键设计：</p><ul><li>以 <code>tool_use_id</code> 为文件名，确保每条输出有唯一存储位置</li><li>幂等：文件已存在则不覆盖</li><li>预览包含 <code>&lt;persisted-output&gt;</code> XML 标签，让模型知道这是被截断的内容</li></ul><p><strong>第二层：微压缩 (<code>micro_compact</code>)</strong></p><p>遍历消息历史，找出所有 <code>role=tool</code> 的消息。保留最近 <code>KEEP_RECENT_TOOL_RESULTS</code> (10 条) 完整不动，将其余每条的内容替换为：</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-"><span class="line"><span>[Earlier tool result compacted. Re-run the tool if you need full detail.]</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>关键设计：</p><ul><li>零成本（纯字符串操作，不调用模型）</li><li>只压缩 <code>content &gt; 120</code> 字符的工具结果，短结果跳过</li><li>占位符明确提示模型&quot;如果需要可以重新运行工具&quot;</li></ul><p><strong>第三层：完整历史压缩 (<code>compact_history</code>)</strong></p><p>调用同一个模型对当前对话生成结构化摘要，然后用一条包含摘要的 user 消息<strong>替换整个消息历史</strong>。</p><p>摘要 prompt (<code>COMPRESS_SYSTEM</code>) 要求模型保留：</p><ol><li>当前目标和剩余工作</li><li>关键发现、决策、错误修复</li><li>读写过的文件（路径 + 简要用途）</li><li>用户约束、偏好、显式指令</li><li>配置值（模型名、API 格式、环境变量）</li></ol><p>同时附加规则防止幻觉：不编造文件路径、不虚构函数名、不捏造项目结构。</p><p>压缩前：</p><ul><li>完整对话以 JSONL 格式写入 <code>.transcripts/</code> 目录（用于审计或恢复）</li><li>摘要后追加 <code>focus</code> 参数（调用方指定的重点）和 <code>recent_files</code>（最近读取的文件列表，方便压缩后重新打开）</li></ul><h3 id="q2-不同的压缩策略在什么时候运行-具体是怎么做的" tabindex="-1"><a class="header-anchor" href="#q2-不同的压缩策略在什么时候运行-具体是怎么做的"><span>Q2: 不同的压缩策略在什么时候运行？具体是怎么做的？</span></a></h3><p><strong>第一层：工具执行后立即运行</strong></p><p>触发时机：每次工具执行完毕后，在将结果写入消息历史之前。</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-"><span class="line"><span>loop.py turn() → 执行工具 → output = persist_large_output(tc.id, output) → 写入消息历史</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>具体做法：</p><ol><li>检查输出长度，<code>&lt;= 30,000</code> 字符直接返回原内容</li><li>超过阈值：创建 <code>TOOL_RESULTS_DIR</code> 目录，以 <code>{tool_use_id}.txt</code> 写入磁盘</li><li>截取前 2,000 字符作为预览</li><li>返回 <code>&lt;persisted-output&gt;</code> 包装的简短占位符（含文件相对路径 + 预览）</li></ol><p>同理，hook 阻止或权限拒绝的短输出也经过 <code>persist_large_output()</code>，确保统一处理。</p><p><strong>第二层 + 第三层（自动）：每轮 API 调用前运行</strong></p><p>触发时机：<code>turn()</code> 函数的 <code>while True</code> 循环顶部，每次发 API 请求之前。</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-"><span class="line"><span>loop.py turn() → while True: → pre_api_compress() → API 调用</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>pre_api_compress()</code> 内部流程：</p><ol><li>先执行 <code>micro_compact()</code> — 压缩旧工具结果（第二层）</li><li>再用 <code>estimate_context_size()</code> 估算上下文大小</li><li>如果超过 <code>CONTEXT_LIMIT</code> (100,000 字符) → 触发 <code>compact_history()</code>（第三层）</li><li>返回压缩后的消息列表</li></ol><p>具体做法：</p><ul><li><code>micro_compact</code>: 收集所有 tool 消息索引 → 保留最后 10 个 → 其余替换为一行占位符</li><li><code>compact_history</code>: 写 transcript 到磁盘 → 调用 <code>summarize_history()</code> 生成摘要 → 构造单条 user 消息 → 更新 <code>CompactState</code></li></ul><p><strong>第三层（手动）：agent 调用 compact 工具时运行</strong></p><p>触发时机：模型在推理中主动调用 <code>compact</code> 工具。</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-"><span class="line"><span>模型返回 tool_calls → 工具执行循环 → 检测到 tool_name==&quot;compact&quot; → manual_compact=True</span></span>
<span class="line"><span>→ 工具循环结束后 → compact_history(client, MODEL, state.messages, COMPACT_STATE, focus=...)</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>具体做法：</p><ol><li><code>registry.py</code> 中 <code>compact</code> 工具的 handler 返回占位符 <code>&quot;Compacting conversation...&quot;</code></li><li>工具循环结束后，<code>turn()</code> 检查 <code>manual_compact</code> 标志</li><li>为 True 则调用 <code>compact_history()</code>，传入模型指定的 <code>focus</code> 参数</li><li><code>compact_history()</code> 执行完整摘要流程（同自动触发）</li></ol><p><strong>辅助：文件访问记录</strong></p><p>触发时机：每次 <code>read_file</code> 工具执行后。</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-"><span class="line"><span>loop.py → 工具执行 → if tool_name==&quot;read_file&quot;: track_recent_file(COMPACT_STATE, path)</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>具体做法：</p><ul><li>将路径追加到 <code>CompactState.recent_files</code> 列表（最多 5 个，LRU 式去重）</li><li>压缩时这些路径被附加到摘要末尾，让 agent 压缩后知道重新打开哪些文件</li></ul>`,55)]])}var s=r(a,[[`render`,o]]);export{i as _pageData,s as default};